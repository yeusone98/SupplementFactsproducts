@model IEnumerable<Supplement_Facts_products.Models.CUSTOMER>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name_Customer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.address_Customer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.phoneNumber_Customer)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name_Customer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.address_Customer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phoneNumber_Customer)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Customer }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Customer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Customer })
        </td>
    </tr>
}

</table>
