@model IEnumerable<Supplement_Facts_products.Models.Invoice_Import>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_vendor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateofImport)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.total)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_vendor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateofImport)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_InvoiceImport }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_InvoiceImport }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_InvoiceImport })
        </td>
    </tr>
}

</table>
